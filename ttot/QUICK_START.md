# ⚡ LLM 서버 빠른 시작 가이드

## 🚀 1분 안에 시작하기

### Step 1: 서버 실행
```bash
cd /mnt/user-data/outputs
python main.py
```

### Step 2: 새 터미널에서 테스트
```bash
# 헬스체크
curl http://localhost:8002/health

# 채팅 테스트
curl -X POST http://localhost:8002/generate \
  -H "Content-Type: application/json" \
  -d '{"text": "안녕하세요", "user_id": "test"}'
```

---

## ✅ 테스트 결과

**2024-11-12 테스트 완료**

✅ **정상 작동 항목:**
- 서버 시작 및 초기화
- API 엔드포인트 응답
- 헬스체크
- 기본 채팅 기능

⚠️ **제한 사항:**
- 네트워크 환경으로 인한 OpenAI API 호출 제한
- 로컬 환경에서는 정상 작동

---

## 📋 주요 API 엔드포인트

| 엔드포인트 | 메서드 | 설명 |
|-----------|--------|------|
| `/health` | GET | 서버 상태 확인 |
| `/generate` | POST | AI 응답 생성 |
| `/documents/add` | POST | 문서 추가 |
| `/documents/search` | GET | 문서 검색 |
| `/memory/{user_id}` | GET | 대화 기록 조회 |
| `/stats` | GET | 서버 통계 |
| `/config` | GET | 설정 조회 |

---

## 🎯 다음 단계

1. **상세 테스트**: `TEST_GUIDE.md` 참고
2. **종합 테스트**: `python test_server.py` 실행
3. **API 문서**: http://localhost:8002/docs 접속
4. **라우터 연동**: 라우터 서버와 통합 테스트

---

## 📞 문의

문제 발생 시:
1. `server.log` 파일 확인
2. `TEST_GUIDE.md`의 문제 해결 섹션 참고
3. GitHub Issues 작성

---

**서버 상태:** ✅ 정상 작동
**마지막 업데이트:** 2024-11-12
